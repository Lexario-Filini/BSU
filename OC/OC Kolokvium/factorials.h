#pragma once
#include <vector>
#include <cstdint>
#include <limits>
#include <string>
#include "errors.h"

namespace core {
	std::vector<std::uint64_t> first_n_factorials(std::uint64_t n);
} 
